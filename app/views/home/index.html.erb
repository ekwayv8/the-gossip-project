<!DOCTYPE html>
<html>

<br/>
<br/>
<div class="jumbotron jumbotron-fluid" style="background-color:#cae3ed">
  <div class="container">
    <h1 class="display-5 text-center" style="color:#37444a" >BIENVENUE à toi!!!!</h1>
    <p class="text-center">Voici la liste de tous les gossips que nous avons en base à ce jour</p>
  </div>
</div>


<% @gossips.each.with_index(1) do |gossip, index| %>
        <p>	
          <%= index %> -
          <%= gossip.user.first_name %><br/>
           "<%=link_to "#{gossip.title}", gossip_path(gossip.id)%>"<br/>
        
        </p>
      <% end %>
</html>
